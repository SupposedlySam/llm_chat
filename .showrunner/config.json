{
  "checks": [
    {
      "cmd": "python3 test/test_pins.py",
      "name": "pins"
    },
    {
      "cmd": "out=$(./zonai compile 2>&1); printf '%s\\n' \"$out\"; case \"$out\" in *'Failed to compile'*) exit 1;; esac",
      "name": "compile"
    },
    {
      "cmd": "python3 test/contract.py",
      "name": "contract"
    },
    {
      "cmd": "python3 test/run.py --tests-only",
      "name": "tests"
    }
  ],
  "collision": {
    "always_serialize": [
      "test/**",
      "tests/**"
    ],
    "extra_globs": []
  },
  "default_lane": "serialized",
  "graph": {
    "backend": "auto",
    "br_db": null,
    "db": ".showrunner/graph.db"
  },
  "harness": {
    "_note": "The per-agent harness is auto-detected. showrunner ASKS it which files are rules and whether a worktree matches its parent \u2014 it keeps no list of its own. Track the harness dir in git (simplest: it then crosses into every worktree by itself), or set \"installer\" to the path of its install script so each worktree is provisioned with the PARENT's rules. Never let a worktree get freshly-seeded rules: an installer seeds user-owned files only when absent, so a blank verify.yaml means a commit gate that owes nothing and reports success.",
    "installer": null,
    "provision": "auto",
    "require": true
  },
  "inject": [
    {
      "mode": "symlink",
      "optional": true,
      "path": ".env"
    }
  ],
  "lanes": [
    {
      "lane": "serialized",
      "match": {
        "labels": [
          "server",
          "serve"
        ],
        "title": "serve|listen|bind|boot"
      },
      "name": "server-work",
      "resource": "loopback-port"
    },
    {
      "lane": "serialized",
      "match": {
        "labels": [
          "device",
          "deploy"
        ],
        "title": "deploy|device|on-device"
      },
      "name": "device-work",
      "resource": "device"
    },
    {
      "lane": "headless",
      "match": {
        "labels": [
          "backend",
          "test",
          "docs",
          "analysis"
        ]
      },
      "name": "pure-logic"
    }
  ],
  "lock_root": null,
  "project_name": "llm_chat",
  "resources": [
    {
      "match": [
        "\\bdeploy\\b",
        "\\bflutter(-tizen)? run\\b"
      ],
      "name": "device",
      "note": "the physical device / deploy target \u2014 one consumer at a time"
    },
    {
      "match": [
        "\\bzonai serve\\b",
        "\\b--port\\s+77\\d\\d\\b"
      ],
      "name": "loopback-port",
      "note": "a bound TCP port on this machine \u2014 one consumer at a time. 7717 is the personal room and 7718 the shared one; both may be LIVE, so a leaf that binds anything in that range serializes rather than racing a real server."
    }
  ],
  "scratch_root": ".showrunner/scratch",
  "shared_state": [],
  "worktree_root": ".worktrees"
}
